import LoginButton from "./LoginButton.jsx";

export default function Solution() {
  return (
    <div>
      <LoginButton />
      <p style={{ marginTop: 12, fontSize: 14 }}>
        🧪 The completed test lives at
        <code> src/days/2026-09-03/LoginButton.solution.test.jsx</code> —
        run <code>npm test</code> in your terminal to see it pass.
      </p>
    </div>
  );
}
